# 应用文档索引（模板）

## 目录导航

- `domain/product/`：主干业务流程、状态与不变量。
- `domain/solution/`：客户、渠道或区域差异。
- `domain/base/`：API、消息、模型与存储入口。
- `tech/`：架构约束、错误处理、性能与运维。

## 新增文档规则

- 新建知识必须先复制 `template/` 对应模板。
- 每个领域文档必须包含元数据块与来源证据。
