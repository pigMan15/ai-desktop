# product 目录说明

## 收录边界

- 主干业务流程：跨状态流转的核心路径。
- 状态：全部合法状态与禁止转换。
- 不变量：任何实现必须满足的约束。

## 命名规则

- 流程：`flow-<动词>.md`。
- 状态：`state-<对象>-lifecycle.md`。

## 禁止

- 不收录单客户或单渠道差异（放 `solution/`）。
- 不复制 base 的接口细节。
