[project]
name = "workflow-platform"
version = "0.1.0"
description = "Local runtime foundation for the AI workflow platform."
requires-python = ">=3.11"
dependencies = [
    "PyYAML>=6.0",
]

[tool.pytest.ini_options]
addopts = "-p no:cacheprovider"
pythonpath = ["src"]
testpaths = ["tests"]
