export function App() {
  return (
    <main className="app-shell">
      <section className="workspace-overview" aria-labelledby="app-title">
        <p className="eyebrow">Workflow Platform</p>
        <h1 id="app-title">AI Workflow Platform</h1>
        <p className="summary">Monorepo foundation is ready for the MVP workbench.</p>
      </section>
    </main>
  );
}
