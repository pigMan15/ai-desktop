{
  "name": "@workflow-platform/desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/main/entry.js",
  "scripts": {
    "dev": "electron .",
    "build": "tsc -p tsconfig.json",
    "test": "node -e \"fs.rmSync('.test-dist',{recursive:true,force:true})\" && tsc -p tsconfig.test.json && node .test-dist/test/main.test.js && node -e \"fs.rmSync('.test-dist',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "electron": "^31.0.0"
  }
}
