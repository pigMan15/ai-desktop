{
  "name": "@workflow-platform/desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "electron .",
    "build": "tsc --outDir dist --rootDir src --target ES2022 --module NodeNext --moduleResolution NodeNext --strict --esModuleInterop --skipLibCheck src/main/main.ts src/preload/preload.ts",
    "test": "tsc --noEmit --target ES2022 --module NodeNext --moduleResolution NodeNext --strict --esModuleInterop --skipLibCheck src/main/main.ts src/preload/preload.ts"
  },
  "devDependencies": {
    "electron": "^31.0.0"
  }
}
